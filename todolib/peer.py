def peer():
    '''Performs actions on Peer objects'''
    #
    # Methods:
    #   peer add [user@domain]
    #       --adds a peer.
    #   peer remove [user@domain]
    #       --removes a peer with the given location.
    #   peer push 
    #       --pushes changelog to all peers.
    #   peer pull 
    #       --pulls changelog from all peers.
    #   peer push [user@domain]
    #       --pushes changelog to specified location.
    #   peer pull [user@domain]
    #       --pulls changelog from specified location.
    #
    pass
